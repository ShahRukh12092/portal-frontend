import { makeStyles } from "@mui/styles";
const useStyle = makeStyles(() => ({
  main: {
    width: "230px",
    height: "40vh",
    margin: "8px",
    borderRadius: "5px",
    boxShadow: "4px",
    position: "relative",
  },
}));

export default useStyle;
